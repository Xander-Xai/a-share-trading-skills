# ERG Merge Readiness

Before merge:

- wire `causal-challenger-model.md` to ERG;
- wire the standard forward-test protocol to the ERG extension/statistical guard;
- update the short/mid-term SKILL output contract if needed;
- verify no Champion/risk parameters changed;
- verify no duplicate/conflicting state semantics remain;
- review PR diff.
